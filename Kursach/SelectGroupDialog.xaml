<Window x:Class="Kursach.SelectGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор группы" 
        Height="450" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="#F0F0F0"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Height" Value="28"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="RowHeight" Value="28"/>
            <Setter Property="AlternatingRowBackground" Value="#F9F9F9"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Выберите группу для назначения старосты" 
                   FontSize="16" 
                   FontWeight="Normal"
                   Margin="0,0,0,15"/>

        <!-- Список групп -->
        <DataGrid Grid.Row="1" 
                  x:Name="GroupsDataGrid"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="GroupsDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Группа" Binding="{Binding GroupName}" Width="120"/>
                <DataGridTextColumn Header="Курс" Binding="{Binding Course}" Width="60"/>
                <DataGridTextColumn Header="Специальность" Binding="{Binding SpecialtyName}" Width="200"/>
                <DataGridTextColumn Header="Студентов" Binding="{Binding StudentCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Выбрать" 
                    Style="{StaticResource ToolbarButton}"
                    Click="SelectButton_Click"/>
            <Button Content="Отмена" 
                    Style="{StaticResource ToolbarButton}"
                    Background="#E1E1E1"
                    Foreground="#333333"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>