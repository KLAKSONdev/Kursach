<Window x:Class="Kursach.AWindows.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Авторизация - Журнал группы колледжа" 
        Height="375" Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#F0F0F0"
        FontFamily="Segoe UI"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="LoginButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Width" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style x:Key="TextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" 
                   Text="Журнал группы колледжа" 
                   FontSize="22" 
                   FontWeight="Bold" 
                   Foreground="#2C3E50"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Иконка -->

        <!-- Логин -->
        <StackPanel Grid.Row="2" Margin="0,10,0,0">
            <TextBlock Text="Логин:" Margin="5,0,0,5" Foreground="#333333"/>
            <TextBox x:Name="LoginTextBox" Style="{StaticResource TextBoxStyle}"/>
        </StackPanel>

        <!-- Пароль -->
        <StackPanel Grid.Row="3" Margin="0,10,0,0">
            <TextBlock Text="Пароль:" Margin="5,0,0,5" Foreground="#333333"/>
            <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordBoxStyle}"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="LoginButton" 
                    Content="Войти" 
                    Style="{StaticResource LoginButtonStyle}"
                   
                    BorderThickness="0"
                    Click="LoginButton_Click"/>
        </StackPanel>

        <!-- Информация -->
        <TextBlock Grid.Row="5" 
                   Text="Для входа используйте учетные данные, полученные у администратора" 
                   FontSize="11" 
                   Foreground="#7F8C8D"
                   TextWrapping="Wrap"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,10"/>
    </Grid>
</Window>